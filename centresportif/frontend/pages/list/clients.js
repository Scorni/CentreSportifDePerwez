import React from 'react';
import PropTypes from 'prop-types';

const clients = props => {
    return (
        <div>
            
        </div>
    );
};

clients.propTypes = {
    
};

export default clients;