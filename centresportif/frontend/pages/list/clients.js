import React from 'react';
import Clients from '../../components/list/Clients'

const clients = props => {
    return (
        <div>
            <Clients />
        </div>
    );
};

clients.propTypes = {
    
};

export default clients;