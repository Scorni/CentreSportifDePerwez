import React from 'react';
import Users from '../../components/list/Users'

const users = props => {
    return (
        <div>
            <Users />
        </div>
    );
};

users.propTypes = {
    
};

export default users;